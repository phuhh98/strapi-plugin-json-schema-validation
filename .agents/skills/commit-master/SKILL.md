---
name: commit-master
description: >
  Automate atomic commit message generation for this workspace, always invoking the "caveman-commit" skill for concise, scoped, and convention-compliant commits. The skill checks the current task scope (from beads or user input), classifies changes by folder, and applies the caveman commit style for all commits. This skill is invoked whenever the user requests to create a commit.
---

# commit-master Skill

Automate atomic commit message generation for this workspace, always invoking the "caveman-commit" skill for concise, scoped, and convention-compliant commits. The skill checks the current task scope (from beads or user input), classifies changes by folder, and applies the caveman commit style for all commits.

---

## Workflow Steps

1. **Determine Task Scope**
   - Check for active beads issues/tasks.
   - If ambiguous, prompt user for current task or scope.

2. **Classify Changes**
   - Changes in `dev/`, `.beads`, `.specify`, `.agents` → `chore` (enforced for non-feature/bug changes to support autonomous release tools that parse conventional commits)
   - Changes in `plugin/` → feature, bugfix, or refactor (ask user if unclear)

3. **Compose Atomic Commits**
   - For each logical change, create a separate commit.
   - Always invoke "caveman-commit" skill for message generation.
   - Each commit message must include a concise subject line (≤50 chars) and a description section below the title. The description section must be a bullet list (not a paragraph), with each bullet describing a logical change, reason, or context. This ensures clarity and supports automated release tools.
   - This skill is invoked automatically whenever the user requests to create a commit (e.g., "commit changes", "make a commit").

4. **Review & Confirm**
   - Present commit messages for user review.
   - Allow user to edit or approve.

5. **Finalize**
   - Commit changes with approved messages.

---

## Quality Criteria

- Each commit is atomic (one logical change per commit).
- Commit messages are generated by "caveman-commit" and match project conventions.
- Each commit message includes a subject line and a bullet list of descriptions clarifying the change (unless the subject is fully self-explanatory). The list can be longer for large or complex changes.
- Chores and features are correctly classified by folder, with 'chore' enforced for non-feature/bug changes to support autonomous release parsing.
- User confirms or edits commit messages before finalizing.

---

## Decisions

- "caveman-commit" skill is always invoked for all commits.
- Chores: changes in `dev/`, `.beads`, `.specify`, `.agents`
- Features/bugfixes: changes in `plugin/`
- No bypass for caveman-commit unless user explicitly requests.

---

## Example Prompts

- "commit all changes for current beads task"
- "compose atomic commits for staged changes"
- "review commit messages before finalizing"

---

## Related Skills to Consider

- pr-description: for generating PR summaries
- beads: for issue tracking and task scoping
- caveman-review: for code review comments

---

## Summary

This skill produces atomic, caveman-style commit messages for all changes, classified by folder and scoped to the current beads task. All commit messages are generated using the "caveman-commit" skill, ensuring brevity and clarity. User review is built-in before finalizing commits.
